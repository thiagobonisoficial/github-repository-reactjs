import styled from 'styled-components';

const Header = styled.header`
    width: auto;
    height: auto;
    display: flex;
    align-items: center;
    font-size: 1.5rem;
    font-weight: bolder;

    svg {
        margin: 0 1rem 0 0;
    }
`;

export default Header;
